import React, { Component } from 'react'
import { WriterWrapper } from "./writer-styled"

export default class Writer extends Component {
    render() {
        return (
            <WriterWrapper>
                Writer
            </WriterWrapper>
        )
    }
}

